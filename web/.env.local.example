# URL of the regrag FastAPI backend.
# When running both via docker-compose, the service name resolves at server-side
# but the browser needs a host-resolvable URL — so we use http://localhost:8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
